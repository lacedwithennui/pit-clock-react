export type CountdownObject = {
    countdownLabel: string;
    countdownValue: string
};

export type AllianceStationHumanReadable = `${"Blue" | "Red"} ${1 | 2 | 3}`;

export type WLTRecordHumanReadable = `${number}-${number}-${number}`;
